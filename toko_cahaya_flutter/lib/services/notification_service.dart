class NotificationService {
  void notify(String title, String body) {
    // Stub: ganti dengan flutter_local_notifications atau Firebase Cloud Messaging
    // Sementara gunakan print, atau tampilkan SnackBar di layer UI.
    // ignore: avoid_print
    print('[NOTIF] $title - $body');
  }
}
